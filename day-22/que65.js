/*            Question 65:
Determine the Remainder: Make a function that gets two numbers and shows the leftover
 from dividing them using the % sign. For example, remainder(5, 2) should give 1.*/
// This function finds the leftover of dividing two numbers
function remainder(num1, num2) {
    // This function finds the leftover of dividing two numbers
    return num1 % num2;
}
var rem = remainder(5, 2);
console.log(rem);
